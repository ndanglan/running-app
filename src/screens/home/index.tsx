import React from 'react';
import {Text} from 'react-native';

import {AppContainer} from 'components/AppContainer';

export const HomeScreen = () => {
  return (
    <AppContainer preset="fixed">
      <Text>THis is Home</Text>
    </AppContainer>
  );
};
