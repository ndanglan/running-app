export const iconRegistry = {
  back: require('./icons/back.png'),
};
export const imageRegistry = {
  //   back: require('./icons/back.png'),
};

export type IconTypes = keyof typeof iconRegistry;
export type ImageTypes = keyof typeof imageRegistry;
